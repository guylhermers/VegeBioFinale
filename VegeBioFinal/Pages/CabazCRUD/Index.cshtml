@page
@model VegeBioFinal.Pages.CabazCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cabaz[0].preco_cabaz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cabaz[0].idCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cabaz[0].epoca_atual)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cabaz) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.preco_cabaz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.epoca_atual)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
